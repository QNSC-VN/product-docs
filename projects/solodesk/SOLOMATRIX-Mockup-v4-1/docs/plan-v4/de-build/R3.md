# ĐỀ R3 — TÍNH NĂNG NÀY CÓ ĐÁNG CÓ KHÔNG: soát thừa · thiếu · sai trọng số

Bạn **KHÔNG sửa code**. Ghi đúng 1 file: `docs/plan-v4/out-build/R3-SOAT-TINH-NANG.md`.
⚠️ 4 file đang được sửa song song (mobile.html, sm-ai.js, sm-b2g.js, sm-seed-b2g.js) — ghi chú nếu thấy dở.

VAI: người giữ phạm vi sản phẩm cho một đội nhỏ. Nguyên tắc: **mỗi tính năng thừa là một quý bị đốt**
(ANTI-SCOPE N-08). Bạn soi từng tính năng đang có và hỏi: cái này có đáng nằm trong bản đi trình tỉnh,
và có đáng nằm trong bản chạy thật của hộ không?

NGUỒN CHUẨN — đọc trước, đây là thước đo:
- `/Users/quangle/.claude/memory-sync/opc-radar/CHARTER.md` (3 trục Quang khoá cứng)
- `/Users/quangle/.claude/memory-sync/opc-radar/REQUIREMENTS.md` (MUST/SHOULD từng trục)
- `/Users/quangle/.claude/memory-sync/opc-radar/ANTI-SCOPE.md` (N-01…N-09)
- `/Users/quangle/Downloads/SoloMatrix_GiaLai_2026-08-17/nguon/THUMOI.txt` (mục II, III, IV, V)

## Việc:
1. **Bảng kiểm kê tính năng** — quét `js/sm-domain.js`, `sm-ops.js`, `sm-quyen.js`, `sm-inbox.js`,
   `sm-program.js`, `sm-onboard.js` + màn tương ứng trong `mobile.html`. Mỗi tính năng 1 dòng:
   tên · màn · **phục vụ trục nào** (A1 kết nối / A2 trong tay / A3 24-7 / tầng Chương trình / không trục nào) ·
   **neo yêu cầu** (R-xx hoặc điều khoản THUMOI hoặc «tự đề xuất») · **hộ dùng bao lâu một lần** ·
   verdict: `LÕI` (bỏ là hỏng sản phẩm) / `ĐỠ` (hỗ trợ lõi) / `TRÌNH DIỄN` (chỉ để hội đồng chấm thấy) /
   `THỪA` (không neo được vào đâu).
2. **Chỉ đích danh phần THỪA và TRÌNH DIỄN**: cái nào nên bỏ hẳn khỏi bản chạy thật, cái nào giữ nhưng
   đưa vào chế độ demo, cái nào giữ nguyên vì đầu bài tỉnh đòi. Có lập luận, không cảm tính.
3. **Thiếu gì mà nghiệp vụ thật cần** — đối chiếu REQUIREMENTS còn `❌`/`🟡` và THUMOI mục IV:
   liệt kê tính năng còn thiếu, xếp theo *mất bao nhiêu điểm nếu hội đồng hỏi mà không có*.
4. **Sai trọng số**: chỗ nào tính năng phụ được đặt to hơn tính năng chính trên màn hình (chiếm chỗ đẹp,
   nút to hơn, lên đầu danh sách) — nêu cụ thể màn + đề xuất đảo lại.
5. **Rủi ro hứa quá tay**: rà mọi chỗ giao diện ngụ ý app làm thay việc pháp lý của hộ (nộp thuế thay,
   ký thay, giữ tiền) — đối chiếu N-06/N-07. Đây là lỗi chết người trước hội đồng, tìm cho kỹ.
6. **Bảng kết luận**: `#` · tính năng · verdict · lý do 1 dòng · hành động đề xuất (giữ/bỏ/gộp/hạ cấp/thêm).
   Cuối bài: **danh sách LÕI tối thiểu** — nếu chỉ được giữ 12 tính năng thì giữ cái nào, và
   **top-5 tính năng nên bỏ ngay**.

## Luật
Mọi verdict phải neo được vào CHARTER/REQUIREMENTS/THUMOI/ANTI-SCOPE hoặc ghi rõ «tự đề xuất».
Không CJK. Xong: dòng cuối `BUILD-AGENT-DONE R3 <số tính năng đã soát>`.
